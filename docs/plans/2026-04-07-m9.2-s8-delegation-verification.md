# M9.2-S8: Delegation Verification — Sprint Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rerun the failed S6 delegation tests now that S7 fixed the root cause (stale tool names, missing skills). This sprint is pure verification — no code changes unless bugs are found.

**Prerequisite:** S7 merged and smoke test passed (brain delegates with correct `create_automation` call).

---

### Step 1: Rerun S6 Step 4 — Forced simple delegation

- [ ] **1a: Test forced delegation**

Open dashboard, send:
> "Check the memory usage of this machine 2 minutes from now and tell me the results."

Verify:
1. Nina calls `create_automation` (NOT answers inline)
2. The call succeeds (todos required by S4 — at least one item present)
3. Automation manifest on disk has `todos` array
4. The todo is meaningful (not garbage)
5. Job fires, worker completes, `status-report.md` written
6. `todos.json` has delegator items + generic template items

### Step 2: Rerun S6 Step 5 — Complex research delegation

- [ ] **2a: Test research delegation**

Open dashboard, send:
> "Find out what the top 3 rated Thai restaurants in Chiang Mai are. Compare their ratings and price ranges."

Verify:
1. Nina delegates via `create_automation` with `job_type: "research"`
2. `todos` array has multiple task-specific items
3. Job runs to completion
4. Worker's `todos.json` has all 3 layers:
   - Layer 1: Nina's task-specific items (from manifest)
   - Layer 2: Research template items (sources, cross-check, chart, status-report)
   - Layer 3: Any worker-added items
5. Worker called `create_chart` for the comparison data (or noted why not)
6. `status-report.md` exists with sources list

### Step 3: Verify S4 schema enforcement in production

- [ ] **3a: Check that todos was enforced**

From the Step 1 and Step 2 results, verify:
1. Every `create_automation` call included `todos` (S4 enforcement)
2. No tool call errors related to missing todos in dashboard logs
3. If any rejection + retry occurred, document the retry behavior

### Step 4: Full end-to-end verification

- [ ] **4a: Run all unit tests**

Run: `cd packages/dashboard && npx vitest run`
Expected: All tests pass.

- [ ] **4b: Verify system prompt correctness**

Check `/api/debug/brain/prompt`:
1. `create_automation` referenced correctly (not `create_task`)
2. Task-triage routing rules present
3. Delegation checklist present
4. Operational rules present
5. No stale `.my_agent/` content duplicating framework skills

### Step 5: Compile final test report

- [ ] **5a: Write test report**

Write `docs/sprints/m9.2-s8-delegation-verification/test-report.md` with:
- Test matrix: all S6 tests rerun, pass/fail
- Delegation behavior: does Nina delegate reliably now?
- Todo quality: are delegator todos meaningful or garbage?
- 3-layer assembly: verified end-to-end?
- S4 enforcement: triggered in production?
- Comparison to S6 findings (all 5 prompts failed → how many pass now?)

- [ ] **5b: Update M9.2 success criteria**

Revisit the success criteria table in `docs/plans/2026-04-06-m9.2-worker-todo-coverage.md`:
- Update "Conversation Nina populating Layer 1 todos" with actual results
- Update any metrics that were previously "UNTESTED"

- [ ] **5c: Commit**

```bash
git add docs/sprints/m9.2-s8-delegation-verification/
git commit -m "docs(m9.2-s8): delegation verification test report

Rerun of failed S6 tests after S7 framework/instance split.
Delegation now works: brain uses create_automation with todos."
```

---

## Success Criteria

| Test | S6 Result | S8 Expected |
|------|-----------|-------------|
| Forced delegation (memory check) | "Scheduled" but no `create_automation` call | Nina delegates via `create_automation` with todos |
| Research delegation (restaurants) | Answered inline with WebSearch | Nina delegates with `job_type: research` + multi-item todos |
| S4 todos enforcement | Never triggered (no delegation occurred) | Todos present on every delegation |
| 3-layer todo assembly | Only tested via disk-write | Verified via real delegation |

**If delegation still fails after S7:** The issue is not stale tool names — it's a deeper design question about whether Sonnet will delegate when it can answer inline. Document findings and escalate to CTO for architectural decision.
